import numpy as np

# テーマ：ブロードキャスト（broadcasting）
# 形の違う配列同士の演算で、NumPy が自動的に形を合わせてくれる仕組み。
# Pandas や機械学習で頻出。「ループを書かずに一括計算する」ための土台。

# 1. 配列とスカラーの演算（最も単純なブロードキャスト）
# - a = np.array([1, 2, 3, 4]) を作る
# - a に 10 を足した結果と、a を 2倍した結果を出力する
# - 👉 スカラー(10) が各要素に「配られている」ことを確認


# 2. 2次元配列 + 1次元配列
# - m = np.arange(6).reshape(2, 3)（2行3列）を作る
# - row = np.array([10, 20, 30])（要素3つ）を作る
# - m + row を出力する
# - 👉 row が各行に足される。形 (2,3) と (3,) が合う理由を考える


# 3. ブロードキャストできない形を体験する
# - bad = np.array([10, 20])（要素2つ）を作る
# - m + bad を try/except で囲んで実行し、出る ValueError を出力する
# - 👉 (2,3) と (2,) は合わない。なぜ失敗するか確認するのが目的


# 4. 列方向のブロードキャスト（縦ベクトルを使う）
# - col = np.array([[100], [200]]) を作る（形は (2, 1) ＝ 2行1列）
# - m + col を出力する
# - 👉 col が各列に足される。row(横) と col(縦) の違いを比べる
